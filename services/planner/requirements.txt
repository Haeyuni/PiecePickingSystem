# TODO: 버전 고정 (착수 시점에 pip freeze로 확정)
fastapi
uvicorn

# LLM/VLM — OpenAI API (개발계획.md D-3). 키는 .env의 OPENAI_API_KEY
openai

# 구조화 출력 스키마 정의 (planner/src/schema.py)
pydantic

# /internal/label-marks가 이미지를 multipart로 받는다. FastAPI의 UploadFile/Form이
# 이것 없이는 import 시점에 죽는다.
python-multipart

# DB (시스템명세서 2절, PostgreSQL 16)
psycopg[binary]

# objects.yaml 시드 로딩 (planner/src/seed.py)
pyyaml

# YOLO 학습용 이미지를 원본 PNG→JPEG로 리인코딩 (app.py:_strip_overlay) +
# 마스크 윤곽선 추출 (vlm_detect.mask_to_polygons).
# 헤드리스(비 GUI) 이미지 프로세싱만 쓰므로 opencv-python-headless로 배포본을 줄인다.
opencv-python-headless

# 마스크 윤곽선(polygon) 좌표 계산 (vlm_detect.mask_to_polygons, numpy 배열 연산)
numpy
