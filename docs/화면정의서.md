# 화면 정의서: web 프론트엔드

`web` 프론트엔드가 실제로 어떤 화면으로 구성되고, 각 화면의 요소가 어떤 인터페이스(REST/WebSocket/ROS2)에서 데이터를 받는지 정의한다. 레이아웃 목업은 대화 중 렌더링된 것을 기준으로 한다.

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 관련 문서 | `웹_인터페이스_정의서.md`, `인터페이스_정의서.md`(sort_msgs), 시스템명세서 |

**화면 구성 원칙**: 실시간 제어 화면과 이력 화면을 완전히 분리한다. "지금 무슨 일이 일어나는가"(실시간, 짧은 수명)와 "예전에 뭘 했는가"(이력, DB 조회, 긴 수명)를 한 화면에 섞으면 둘 다 읽기 어려워진다.

---

## 1. 화면 목록

| 화면 | 경로(예시) | 성격 |
|---|---|---|
| 제어 화면 | `/` | 실시간, WebSocket 기반, 단일 사용자 가정 |
| 이력 화면 | `/history` | 정적 조회, REST 폴링/새로고침 기반 |

두 화면은 상단 네비게이션(제어 화면의 "이력 보기" 링크, 2.1절)으로만 연결되고, 상태를 공유하지 않는다 — 이력 화면은 자체적으로 `GET /api/executions`를 호출해 독립적으로 그린다.

---

## 2. 제어 화면

### 2.1 레이아웃 영역

```
┌─────────────────────────────────────────────────────────┐
│ 타이틀                                    [이력 보기 →]   │  상단 네비
├─────────────────────────────────────────────────────────┤
│ ● Idle   현재 스킬 없음              연결됨               │  상태바
├─────────────────────────────────────────────────────────┤
│ ⚠ (조건부) 안전 이벤트 배너                                │  안전 배너
├───────────────────────────┬─────────────────────────────┤
│ [카메라 뷰]  [뎁스 뷰]      │ 탐지 물체 목록                │
│                           │ ─────────────                │
│                           │ [홈 이동]      [정지]         │  메인 그리드
├───────────────────────────┴─────────────────────────────┤
│ 현재 작업 — "명령 텍스트"                                  │
│  Pick — 물체명       [██████████░░] 검증 중                │  작업 진행도
│  Place into — 목적지 [░░░░░░░░░░░░] 대기 중                │
├─────────────────────────────────────────────────────────┤
│ [명령 입력창 · 마이크 · 전송]                              │  명령 입력
│ (비활성 시) 사유 문구                                       │
└─────────────────────────────────────────────────────────┘
```

### 2.2 컴포넌트별 정의

#### 2.2.1 상단 네비게이션

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 타이틀 | 정적 텍스트 | — |
| 이력 보기 링크 | `/history`로 이동 | — |

#### 2.2.2 상태바

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 모드 배지 | `idle`(초록)/`busy`(파랑)/`error`(주황)/`estopped`(빨강), 점+텍스트 | WebSocket `robot_state.mode` |
| 현재 스킬 | "No active skill" 또는 `current_skill` 값 | WebSocket `robot_state.current_skill` |
| 연결 상태 | WebSocket 연결 여부 | 프론트 자체 상태(연결 끊김 시 "연결 끊김"으로 전환, 재연결 시도) |

#### 2.2.3 안전 배너

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 배너 (조건부, `severity=critical`일 때만 노출) | 아이콘 + `description` | WebSocket `safety_event` |
| 해제 | `requires_manual_reset=false`이거나 새로운 `robot_state.mode=idle` 수신 시 자동 숨김 | — |

**우선순위**: 다른 모든 WebSocket 이벤트보다 먼저 렌더링·먼저 눈에 띄어야 한다(`웹_인터페이스_정의서.md` 4절 원칙). `severity=warning`은 배너 대신 상태바 옆 작은 아이콘으로 축소 표시(토스트 등으로 격하하지 않음 — 놓치면 안 되므로 상시 배치, 다만 크기만 작게).

#### 2.2.4 카메라 · 뎁스 뷰

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 카메라 뷰 | RGB 원본 스트림 | 카메라 드라이버(외부 의존성, `인터페이스_정의서.md` 2절) → `web`이 스트리밍 중계 |
| 뎁스 뷰 | 뎁스 맵 시각화(컬러맵) | 동일 |

**세그멘테이션 마스크는 오버레이하지 않는다** — 앞서 결정한 대로, 마스크는 `grasp` 내부 비공개 데이터이자 UI 표시 필요성이 없다고 판단했다. 원본 스트림만 보여준다.

#### 2.2.5 탐지 물체 목록

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 목록 헤더 | "탐지된 물체 (N)" | `WorldState.objects.length` |
| 각 항목 | `name_ko`, `profile` 배지(색상: fragile=주황/normal=파랑/deformable=보라) | `WorldState.objects[]` (WebSocket 또는 최신 `/world_state` 스냅샷) |
| `needs_confirmation=true` 항목 | 별도 배지("확인 필요") + 클릭 시 확인 모달(2.2.7) | `WorldState.objects[].needs_confirmation` |

#### 2.2.6 로봇 직접 제어 버튼

| 요소 | 내용 | 데이터 출처 / 동작 |
|---|---|---|
| 홈 이동 | `robot_state.mode !== 'idle'`이면 비활성화 | `POST /api/robot/home` (`웹_인터페이스_정의서.md` 2.6절) |
| 정지 | **항상 활성화** — mode와 무관 | `POST /api/robot/stop` (2.6절, busy 차단 규칙의 예외) |

정지 버튼은 위험 강조 스타일(빨강 계열 테두리/텍스트)로 시각적으로 구분한다 — 다른 버튼과 같은 톤이면 급할 때 못 찾는다.

#### 2.2.7 신규 물체 확인 모달 (FR-05b)

탐지 목록에서 "확인 필요" 항목 클릭 시 노출. 별도 화면이 아니라 제어 화면 위 모달이다(맥락 전환 없이 바로 처리).

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 크롭 이미지 | VLM 제안의 근거가 된 이미지 | `image_ref` (`GET /api/object-confirmations`) |
| 제안된 속성 | 이름·재질·파손위험 등, 수정 가능한 폼 | `suggested_*` 필드 |
| 승인/수정 버튼 | 제출 시 확정 | `POST /api/object-confirmations/{class_name}` |

이 모달이 떠 있는 동안에도 명령 입력은 별개로 동작한다 — 신규 물체 확인은 승인 절차가 아니라 속성 확정이므로, 다른 명령 실행을 막지 않는다(단, 그 물체는 확인 전까지 `fallback` 프로파일로만 다뤄진다).

#### 2.2.8 작업 진행도

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 현재 명령 텍스트 | `command_text` | `GET /api/traces/{trace_id}` 또는 명령 전송 시 로컬 보관 |
| 스텝별 진행바 | 스킬명 + 물체/목적지 + `phase` 텍스트 | WebSocket `execution_progress`(`phase`), `execution_result`(완료 시) |
| 진행바 채움 정도 | `phase` 값을 스킬별 4단계로 매핑(예: `pick`은 approaching=25%/contact_detected=50%/lifting=75%/verifying=90%, 완료 시 100%) | 프론트 자체 매핑 테이블 |
| 시퀀스 없음 상태 | "대기 중인 작업이 없습니다" | — |

#### 2.2.9 명령 입력창

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 텍스트 입력 | 자연어 명령 | `POST /api/commands` |
| 마이크 버튼 (2차) | 클릭 시 녹음 시작/종료, 종료 시 `/api/stt` 호출 → 결과를 입력창에 채움(자동 전송 아님, FR-24) | `POST /api/stt` |
| 전송 버튼 | `robot_state.mode !== 'idle'`이면 비활성화 | `POST /api/commands` |
| 비활성 사유 문구 | "로봇이 동작 중입니다 — 완료 후 다시 시도하세요" (mode별로 문구 분기: busy/error/estopped) | `robot_state.mode` |

`low_confidence=true`(STT 신뢰도 0.8 미만, `웹_인터페이스_정의서.md` 2.2절)면 입력창에 채우되 "인식이 불확실합니다. 확인 후 전송하세요" 경고를 같이 표시한다.

---

## 3. 이력 화면

### 3.1 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│ 이력                                        [← 제어 화면]  │
├─────────────────────────────────────────────────────────┤
│ 필터: [기간] [결과: 전체/성공/실패] [스킬: 전체/pick/.../home/stop] │
├─────────────────────────────────────────────────────────┤
│ 시각        명령/스킬          결과   파지전략   시각검증    │
│ ─────────────────────────────────────────────────────── │
│ 09-03 05:11 "깨지기 쉬운 것만..." 성공  contact_gr..  통과   │  › 상세
│ 09-03 05:09 stop                 —     —          —      │  › 상세
│ ...                                                       │
└─────────────────────────────────────────────────────────┘
```

### 3.2 컴포넌트별 정의

| 요소 | 내용 | 데이터 출처 |
|---|---|---|
| 필터 | 기간, 결과(성공/실패), 스킬 종류(`pick`/`place_into`/`home`/`stop`) | 프론트 상태, `GET /api/executions` 쿼리 파라미터로 전달(쿼리 파라미터 확장 필요 — 8절 참조) |
| 목록 행 | 시각, 명령 텍스트(`stop`/`home`은 명령 텍스트 없이 스킬명만), 결과, `grasp_strategy`, `visual_verification_passed` | `execution_logs` (`GET /api/executions`) |
| 상세 (행 클릭 시 펼침) | 같은 `trace_id`의 전체 스텝, `torque_trace` 요약, `bin_id`, `retry_count`, `cycle_time_ms` | `GET /api/traces/{trace_id}` 또는 `execution_logs` 상세 |

**`stop`/`home` 이력도 같은 테이블에 섞여 나온다** — `command_text`가 없는 행으로 표시하고 스킬명(`stop`/`home`)을 대신 보여준다. 별도 탭으로 분리하지 않는다. 분류 작업과 로봇 직접 제어를 시간순으로 같이 봐야 "정지 버튼을 왜 눌렀는지" 맥락 파악이 쉽다.

**신규 물체 확인 이력**은 이 화면에 포함하지 않는다 — `execution_logs`가 아니라 `object_attributes`의 변경 이력이라 성격이 다르다. 필요해지면 별도 화면으로 분리한다(8절 미확정 항목).

---

## 4. 화면 간 공통 규칙

| 규칙 | 내용 |
|---|---|
| 색상 의미 | 초록=idle/성공, 파랑=busy/normal, 주황=fragile/warning, 빨강=error·estop/실패, 보라=deformable — 두 화면에서 동일 색상은 항상 동일 의미 |
| 시간 표시 | 상대 시간이 아니라 절대 시각(로봇 동작은 순서·정확한 시각이 중요) |
| 빈 상태 | "탐지된 물체 없음", "대기 중인 작업 없음", "이력 없음" 등 — 빈 배열을 빈 화면으로 방치하지 않는다 |

---

## 5. 미확정 항목

- `GET /api/executions`의 쿼리 파라미터가 현재 `trace_id`/`limit`뿐이다(`웹_인터페이스_정의서.md` 2.5절) — 이력 화면의 필터(기간, 결과, 스킬 종류)를 지원하려면 파라미터 확장이 필요하다. `웹_인터페이스_정의서.md`도 함께 갱신해야 한다.
- 신규 물체 확인 이력을 별도 화면으로 만들지 여부
- 카메라·뎁스 스트림을 `web`이 어떤 방식으로 브라우저에 전달할지(MJPEG, WebRTC, 프레임 단위 WebSocket 등) — 지연시간 요구에 따라 선택 필요, 아직 미정
- 다중 물체 동시 작업 시(향후 확장) 작업 진행도 영역이 여러 시퀀스를 동시에 어떻게 표시할지 — 1차 범위는 단일 시퀀스 가정