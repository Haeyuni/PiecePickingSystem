# TODO: 버전 고정 (착수 시점에 pip freeze로 확정)
fastapi
uvicorn[standard]     # WebSocket 지원 포함

# planner(/internal/plan) 호출
httpx

# execution_logs / object_attributes 직접 접근 (웹_인터페이스_정의서 2.5절)
psycopg[binary]

# 카메라·뎁스 뷰 JPEG 인코딩 (ros_bridge.py, 화면정의서 2.2.4절). GUI 없는 컨테이너라
# headless 빌드 — perception과 달리 여기선 opencv-python(비headless)이 요구하는 libGL이
# 필요 없다.
opencv-python-headless
numpy

# STT (stt.py, 웹_인터페이스_정의서 2.2절, FR-23~25). CTranslate2 기반이라 GPU 없이도
# CPU INT8 양자화로 돌아간다 — web 컨테이너에는 GPU를 안 준다.
faster-whisper

# 주의: rclpy는 여기에 없다. ROS2 환경(ros:jazzy 베이스)에서만 제공되며,
# MOCK_MODE=0일 때 ros_bridge.py에서만 import된다 (web/Dockerfile 참조).
